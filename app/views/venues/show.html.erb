<h1 align="center">Title : <%= @venue.title %></h1>
	<div>
		<h4 align="center">Place :<%= @venue.place %> </h4>	

	</div>
   	 <div>
		   <%= link_to "Edit this venue" , edit_venue_path(@venue), class: "btn btn-xs btn-primary" %>
			<%= link_to "Delete this venue", venue_path(@venue), method: :delete, data: {confirm: "Are you sure you want to delete this venue ?"}, class: "btn btn-xs btn-danger" %>
			<%= link_to "View all venues" ,venues_path, class: "btn btn-xs btn-success" %>

	</div>